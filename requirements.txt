# This repo's Python project lives in `trading_bot/` (see `trading_bot/pyproject.toml`).
# These are the runtime dependencies from `[project].dependencies`.
#
# For an editable install of the project itself, run:
#   pip install -e trading_bot

MetaTrader5>=5.0.45
numpy>=1.26
pandas>=2.2
pydantic>=2.6
python-dotenv>=1.0
PyYAML>=6.0
psutil>=5.9
requests>=2.31
rich>=13.7
textual>=0.58

